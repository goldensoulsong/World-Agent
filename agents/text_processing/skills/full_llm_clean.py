FULL_LLM_CLEAN_SKILL = {
    "name": "full_llm_clean",
    "display_name": "大模型并发全量清洗 (Full LLM Batch Clean)",
    "trigger_condition": "当用户明确要求“大模型全量清洗”、“并发精洗”、“使用大模型彻底洗一遍”时触发此技能。",
    "sop": (
        "你已进入【大模型全量清洗高级工作流】。请务必严格按照以下 4 步执行，绝不可跨越步骤：\n\n"
        "第一步：背景与高发污染特征核实 (Context Alignment)\n"
        "- 拒绝立刻调用任何清洗工具。主动询问用户两件事：\n"
        "  1. 这批语料是什么类型？（例如：小说正文、设定集、系统日志等）\n"
        "  2. 重点污染特征是什么？引导用户思考高发病灶。例如直接询问：“请问这次的垃圾数据有没有经常出现在每个切片的最后一句话？（这是防盗版和求票的高发区）或者是段首有固定的乱码？”\n\n"
        "第二步：动态清洗策略生成与预检展示 (Proposal Draft)\n"
        "- 当用户回答了背景特征后，你需要基于其反馈，为你自己自动起草一套最严密的 `clean_instruction`。\n"
        "- 向用户展示你起草的 `clean_instruction`。\n"
        "- 【智能参数决策】你必须根据以下维度自主推理出最佳的 `concurrency`（并发数）和 `chunks_per_batch`（单次合并块数），而不是使用固定值：\n"
        "  * 总切片数量：切片越多，可适当提高并发以缩短总耗时。\n"
        "  * 单个切片的平均长度：切片越长，chunks_per_batch 应越小，防止单次请求超出大模型上下文窗口或导致输出被截断。如果平均切片很短（<500字），chunks_per_batch 可以大胆设到 30-50；如果切片较长（>2000字），应保守地设为 5-10。\n"
        "  * 参考区间：concurrency 一般在 3~10 之间（默认 5），chunks_per_batch 一般在 5~50 之间（默认 20）。\n"
        "- 向用户展示你推理出的参数值及其理由，供用户确认或调整。\n\n"
        "第三步：底层抽检试运行 (Sample Preview)\n"
        "- 在展示了策略并征得初步同意后，**必须**调用 `sample_llm_clean_config` 工具进行一次真实的大模型试运行抽检。\n"
        "- 将工具需要的四个参数：`input_json_path`（如果你不知道，默认使用 workspace/output 下刚刚切好的 json 文件，或者问用户）、`concurrency`、`chunks_per_batch` 和你刚才起草的 `clean_instruction` 传给该工具。\n"
        "- 把工具返回的试洗局部对比（洗前 vs 洗后）、预估消耗展示给用户。\n\n"
        "第四步：最终拍板与全量并发执行 (Execution)\n"
        "- 试洗样本展示后，明确询问用户：“是否确认【拍板执行】全量并发清洗任务？”\n"
        "- 只有当用户明确回复同意后，才调用终极工具 `llm_batch_clean_text`，传入刚才一样的参数，启动最终作业。"
    )
}
