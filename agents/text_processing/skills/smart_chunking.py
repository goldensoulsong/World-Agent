SMART_CHUNKING_SKILL = {
    "name": "smart_chunking",
    "display_name": "智能文本切块 (Smart Chunking)",
    "trigger_condition": "当用户要求对文本、文档或小说进行切块、分割、分片时触发此技能。",
    "sop": (
        "1. 【安全规范与抽样探查】：所有的操作被严格限制在沙盒区。调用相关阅读工具对存放在 `workspace/input/` 目录下的目标文件进行前 1000 行以及尾部探查，提取核心排版特征。\n"
        "2. 【结构规范定义】：诊断文件类型并确立 `title` 提取规范：\n"
        "   - **网文/小说格式**：如果文件包含类似于“第X章”或“☆、第X章”的格式，title 必须被定义为完整的章节名称。\n"
        "   - **Markdown格式**：由于底层工具使用正则表达式平铺切分，无法原生保留父子层级（如 # 标题1 > ## 标题2）。你必须告知用户这一局限性，并约定以局部最近的标题作为当前切片的 title。\n"
        "   - **其他散文/平铺格式**：明确告诉用户由于缺乏显式标题，将使用系统默认的“区块X”作为 title。\n"
        "3. 【正则推演与提案】：\n"
        "   - 为该结构编写最合适的正则表达式 `split_pattern`。\n"
        "   - **极其重要**：如果需要提取标题，你的正则必须包含捕获组 `()` 以包裹你要作为 title 的部分！如果没有包含 `()`，工具会进行盲切，可能导致标题被孤立成单独的废块。\n"
        "   - 必须向用户展示【沙盘推演示例】，即：“使用此正则，预期的切片结果示例为：{title: '示例标题', content: '示例正文...'}”。\n"
        "   - **【碎片处理决策权】**：如果你发现章节被切成了碎片（例如“第一章(一)”、“第一章(二)”），你**绝不能自作主张替用户决定**！因为用户的目的可能是存入知识库做 RAG 检索（适合保持碎片），也可能是为了排版看小说（希望能合并成一整章）。你必须在提案时把情况汇报给用户，给出选项，把【是否合并】的最终选择权交给用户。\n"
        "4. 【强制交互挂起】：向用户报告你的发现、结构规范以及沙盘推演示例。然后**强制停止动作**，等待用户回复“同意”或提供最终处理意愿。严禁未经同意盲目执行 `chunk_text`。\n"
        "5. 【执行与质检】：获得同意后执行切块。完成后，主动读取切出的 JSON 结果文件的前 2 个块，严厉核对 title 和 content 是否错位（如 title是区块X但content仅剩标题）。如果正确，向用户汇报成功；如果有误，立刻向用户报告错误并提议重试。"
    )
}
