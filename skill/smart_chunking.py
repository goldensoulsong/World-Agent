SMART_CHUNKING_SKILL = {
    "name": "smart_chunking",
    "display_name": "智能文本切块 (Smart Chunking)",
    "trigger_condition": "当用户要求对文本或小说进行切块、分割、分片时触发此技能。",
    "sop": (
        "1. 【探查】：调用 read_file 工具对目标文件进行安全探查（如果在当前项目目录下，也可以直接读取）。\n"
        "2. 【分析】：诊断文件的格式结构。例如，它是用 `#` 标题划分的 Markdown？是用 `\\n\\n` 换行划分的散文？还是有明确卷章号的小说？\n"
        "3. 【提案】：为你诊断出的格式编写一个最合适的正则表达式 `split_pattern`。\n"
        "   注意：如果你传入的正则包含捕获组 `()`，在 chunk_text 工具中，捕获组的内容将被作为后续每个 chunk 的 title；如果不需要专门的 title，就不要加捕获组。\n"
        "4. 【强制交互】：向用户报告你的发现，展示你拟定的正则表达式或切分策略，然后**强制停止动作**，等待用户回复“同意”或修改意见。\n"
        "5. 【执行】：获得用户同意后，调用 chunk_text 工具，并使用 `split_pattern` 参数传入你写好的正则。如果没有收到明确同意，严禁盲目执行 chunk_text。\n"
        "6. 【质检】：切块完成后，主动调用 read_chunk 读取刚刚生成的文件的前 2 块进行质检，确认格式正确后向用户汇报大功告成。"
    )
}
